#include<stdlib.h>
#include<unistd.h>
#include<stdio.h>
int main()
{
FILE *rd;
char buff[100];
printf(buffer,"name first");
fd=popen("wc _c","w");
fwrite(buffer,sizeof(char),strlen(buffer),rd);
pclose(rd);
}
